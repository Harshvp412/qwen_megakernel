# Copy to .env and adjust. Optional: demo_pipecat.py loads .env via python-dotenv.
#
# --- Free local models (no API keys) ---
# Whisper STT: tiny = fast download on first run (good for GPU server), base = better quality
WHISPER_MODEL=tiny
# Ollama LLM model (must be pulled: ollama pull llama2)
OLLAMA_MODEL=llama2
#
# --- Or use API keys (comment out above, set below) ---
# DEEPGRAM_API_KEY=your_deepgram_key
# OPENAI_API_KEY=your_openai_key
#
# --- Optional ---
# PORT=7860
# USE_LEGACY_TTS=1
# RECORD_DEMO=1
